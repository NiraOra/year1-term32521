//

/*
int itemCmp (Item a, Item b) {
  // if f < s, return -1
  if (a.k < b.k) {
    return -1;
  } 
  // if f > s, return 1
  if (a.k > b.k) {
    return 1;
  }
  // equal check
  if (a.k == b.k) {
    // if f < s, return -1
    if (a.j < b.j) {
    return -1;
    } 
    // if f > s, return 1 
    else if (a.j > b.j) {
      return 1;
    }
    else {
      return 0;
    }
     
  }

}

*/